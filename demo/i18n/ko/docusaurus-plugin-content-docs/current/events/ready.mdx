---
sidebar_position: 3
---

import View360, { EVENTS } from "@site/src/components/View360";

```ts
import { EVENTS, ReadyEvent } from "@egjs/view360";

// typescript 코드입니다.
const onReady = (evt: ReadyEvent) => {};

view360.on(EVENTS.READY, onReady);
```

컴포넌트 초기화시 발생하는 이벤트
이 이벤트는 [init()](/docs/api/Class/View360#init)이 호출된 이후 단 1회만 호출됩니다.

:::info
아래 예제는 이벤트 순서 확인을 위해 의도적으로 모든 컨트롤을 비활성화했습니다.

아래 예제에서는 초기화시 트리거되는 모든 이벤트들을 확인 가능합니다.
:::

<View360
  projectionOptions={{
    src: "/pano/equirect/veste.jpg"
  }}
  license="veste"
  clickToLoad={true}
  rotate={false}
  zoom={false}
  gyro={false}
  showControlBar={false}
  showEvents={Object.values(EVENTS)} />
