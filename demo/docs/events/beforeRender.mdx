---
sidebar_position: 8
---

import View360 from "@site/src/components/View360";

```ts
import { EVENTS, BeforeRenderEvent } from "@egjs/view360";

// Typescript
const onBeforeRender = (evt: BeforeRenderEvent) => {};

view360.on(EVENTS.BEFORE_RENDER, onBeforeRender);
```

An event that fires before rendering a frame.

:::info
In the example below, only the "beforeRender" event is visible.
:::

<View360
  projectionOptions={{
    src: "/pano/equirect/veste.jpg"
  }}
  license="veste"
  showEvents={["beforeRender"]} />
